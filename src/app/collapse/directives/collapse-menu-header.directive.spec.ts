import { CollapseMenuHeaderDirective } from './collapse-menu-header.directive';

describe('CollapseMenuHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new CollapseMenuHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
